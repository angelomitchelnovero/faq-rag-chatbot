# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic>=2.11.7,<3.0.0
pydantic-settings==2.5.2

# --- RAG / Vector DB ---
# Note: embeddings run locally via fastembed (ONNX, no PyTorch, no API
# cost). Vectors are stored in Postgres via pgvector rather than a
# separate local ChromaDB, so they persist through backend restarts on
# hosts with ephemeral disk (e.g. Render's free tier).
fastembed==0.8.0
pgvector==0.5.0

# --- Database (Supabase Postgres) ---
sqlalchemy==2.0.35
psycopg2-binary==2.9.12

# --- File storage (Supabase Storage) ---
supabase==2.31.0

# --- PDF parsing/generation ---
pypdf==4.3.1
pdfplumber==0.11.4
reportlab==4.2.5

# --- Gemini LLM ---
# Note: google-generativeai was deprecated by Google (Nov 2025) in favor of
# this unified SDK. Free tier (as of mid-2026) covers Flash and Flash-Lite models.
google-genai==1.2.0

# --- Auth ---
python-jose[cryptography]==3.3.0
bcrypt==4.2.0

# --- Env config ---
python-dotenv==1.0.1
