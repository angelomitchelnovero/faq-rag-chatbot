# --- Web framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9
pydantic==2.9.2
pydantic-settings==2.5.2

# --- RAG / Vector DB ---
# Note: ChromaDB's default embedding function (all-MiniLM-L6-v2 via ONNX Runtime)
# is used instead of sentence-transformers/PyTorch. Same embedding quality,
# ~10x smaller install and much friendlier to free-tier hosting (Render/Railway).
chromadb==0.5.15
onnxruntime==1.19.2

# --- PDF parsing ---
pypdf==4.3.1
pdfplumber==0.11.4

# --- Gemini LLM ---
google-generativeai==0.8.3

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# --- Database (SQLite for now) ---
sqlalchemy==2.0.35

# --- Env config ---
python-dotenv==1.0.1
